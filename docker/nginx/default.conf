server {
	listen 80;
	server_name agorachain.es www.agorachain.es;

	location / {
		return 301 https://$host$request_uri;
	}

	location /.well-known/acme-challenge {
		root /var/www/certbot;
	}
}

server {
	listen 443 ssl;
	server_name agorachain.es www.agorachain.es;

    # Certificado generado con letsencrypt
    ssl_certificate /etc/letsencrypt/live/agorachain.es/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/agorachain.es/privkey.pem;

    # AUTENTICACIÓN POR CERTIFICADO
    # No se puede meter en un location o da fallo
    # Pensar en como ponerlo para que aparezca solo al pinchar en INGRESAR
    ssl_client_certificate /etc/nginx/certs/fnmt-chain.pem;
    ssl_verify_client optional;
    ssl_verify_depth 2;

	root /var/www/frontend;
	index index.html;

	location / {
		try_files $uri $uri/ /index.html;
	}

    location /api/ {
        alias /var/www/html/public/;
        index index.php;

        try_files $uri $uri/ @laravel;

        location ~ \.php$ {
            fastcgi_pass backend:9000;
            fastcgi_param SCRIPT_FILENAME /var/www/html/public/index.php;
            include fastcgi_params;
            
            fastcgi_param HTTPS on;
            fastcgi_param REQUEST_URI $request_uri;

            # Envio el certificado digital al backend, por esta razón me daba el error 403
            fastcgi_param SSL_CLIENT_VERIFY   $ssl_client_verify;
            fastcgi_param SSL_CLIENT_S_DN     $ssl_client_s_dn;
            fastcgi_param SSL_CLIENT_SERIAL   $ssl_client_serial;
            fastcgi_param SSL_CLIENT_CERT     $ssl_client_escaped_cert;
        }
    }

    location @laravel {
        fastcgi_pass backend:9000;
        include fastcgi_params;
        fastcgi_param SCRIPT_FILENAME /var/www/html/public/index.php;
        fastcgi_param HTTPS on;
        fastcgi_param REQUEST_URI $request_uri;

        # Certificado
        fastcgi_param SSL_CLIENT_VERIFY   $ssl_client_verify;
        fastcgi_param SSL_CLIENT_S_DN     $ssl_client_s_dn;
        fastcgi_param SSL_CLIENT_SERIAL   $ssl_client_serial;
        fastcgi_param SSL_CLIENT_CERT     $ssl_client_escaped_cert;
    }
}
