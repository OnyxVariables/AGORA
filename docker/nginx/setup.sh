#!/bin/sh

# DOMAIN="agorachain.es"
# SSL_DIR="/etc/ssl/"

# mkdir -p $SSL_DIR

# openssl req -x509 -nodes -days 365 \
#     -newkey rsa:2048 \
#     -keyout "$SSL_DIR/private/privkey.pem" \
#     -out "$SSL_DIR/certs/fullchain.pem" \
#     -subj "/CN=$DOMAIN"

# exec "$@"
